// Stub component. Imported by app/home/page.tsx but the original source
// was never synced down from v0. Returning null keeps the home page
// rendering and unblocks the build (and the /api/mobile-chat route deploy).
// Fill in real implementation when reminder banner UX is finalized.
"use client"

export function ReminderBanner() {
  return null
}
